{
  "name": "teacher-quest-2569",
  "version": "4.1.1",
  "private": true,
  "scripts": {
    "test": "npm run validate && npm run test:smoke",
    "validate": "node scripts/validate.mjs",
    "test:smoke": "playwright test tests/smoke.spec.cjs --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "1.54.1"
  }
}
